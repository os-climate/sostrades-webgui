import { MarkdownRendererDirective } from './markdown-renderer.directive';

describe('MarkdownRendererDirective', () => {
  it('should create an instance', () => {
    const directive = new MarkdownRendererDirective();
    expect(directive).toBeTruthy();
  });
});
